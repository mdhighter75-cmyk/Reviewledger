# Required — powers every specialist role (planning, book, website copy,
# marketing, video script, launch strategy) and is the fallback whenever a
# preferred specialist tool below is unavailable.
ANTHROPIC_API_KEY=

# Optional — if set, the Branding specialist generates an actual logo/cover
# image via OpenAI's image API instead of a text-only design brief.
OPENAI_API_KEY=
